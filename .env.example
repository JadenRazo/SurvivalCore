# SurvivalCore Deployment Configuration
# Copy to .env and fill in real values

# Dev Server SFTP
DEV_SFTP_HOST=
DEV_SFTP_PORT=22
DEV_SFTP_USER=
DEV_SFTP_PASSWORD=
DEV_SERVER_PATH=/path/to/dev/server

# Production Server SFTP (same as SMP in monorepo)
SMP_SFTP_HOST=
SMP_SFTP_PORT=
SMP_SFTP_USER=
SMP_SFTP_PASSWORD=
PROD_SERVER_PATH=/path/to/production/server
